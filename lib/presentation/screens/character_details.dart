
import 'package:flutter/material.dart';

class CharactersDetalisScreen extends StatelessWidget {
  const CharactersDetalisScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
